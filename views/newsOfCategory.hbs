<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>
    <script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
    <link rel="stylesheet" href="../mystyle.css">
    <title>News of each category</title>
    <style>
        .title a {
            font-size: 20px;
            font-weight: bold;
        }
        .text a{
            color: #888;
            font-size: 14px;
            font-weight: normal;
        }
        .titleHotNews a{
            font-size: 16px;
            font-weight: normal;
        }
        .container-fluid {
            background-color: #B00020;
        }

        .nav-link {
            color: white;
        }

        .nav-link:hover {
            background-color: #9a0007;
            color: white;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-sm ">
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="/"><i class='fas fa-home'
                            style='font-size:24px;color:rgb(255, 255, 255)'></i></a>
                </li>
                {{#each category}}
                <li class="nav-item">
                    <a class="nav-link" href="/newsOfCategory?id={{_id}}">{{name}}</a>
                </li>
                {{/each}}
            </ul>
            {{#if checkLogin}}
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </li>
            </ul>
            {{else}}
            <ul class="navbar-nav">
                <li class="nav-item" style="text-align: left;padding-top: 6px;">
                    <a class="nav-link" href="/user/chargeMoney">
                        <i class="fa fa-plus-circle"> {{user.coin}} $</i>
                    </a>
                </li>
                <div class="btn-group">
                    <button type="button" class="btn dropdown-toggle" data-bs-toggle="dropdown">
                        <i class='fas fa-user-circle' style='font-size:36px;color:white'></i>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li class="nav-item" style="text-align: left;">
                            <a class="nav-link" href="/user/infoUser" style="color:black">Profile</a>
                        </li>
                        <li class="nav-item" style="text-align: left;">
                            <a class="nav-link" href="/logout" style="color:black">Log out</a>
                        </li>
                    </ul>
                </div>
            </ul>
            {{/if}}
        </div>
    </nav>
    <div class="row justify-content-center">
        <div class="col-6">
            <div class="itemCategory">
                {{#each data}}
                <div class="col-5">
                    <a href="/detailNewsOfWriter?id={{_id}}"><img src="../images/{{files.filename}}" width="100%" alt=""></a>
                </div>
                <div class="col-7">
                    <p class="title"><a href="/detailNewsOfWriter?id={{_id}}">{{title}}</a></p>
                    <p class="text"><a href="/detailNewsOfWriter?id={{_id}}">{{text}}</a></p>
                </div>
                {{/each}}
            </div>
        </div>
        <div class="col-2">
            {{#each newsOfHotNews}}
            <div>
                <a href="/detailNewsOfWriter?id={{_id}}"><img src="../images/{{files.filename}}" width="100%" alt=""></a>
                <p class="titleHotNews"><a href="/detailNewsOfWriter?id={{_id}}">{{title}}</a></p>
            </div>
            {{/each}}
        </div>
    </div>
</body>

</html>